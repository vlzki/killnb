<%= simple_form_for :selection, url: assets_path, method: :get do |f| %>
  <%= f.input :city, label: false, placeholder: 'city' %>
  <%= f.input :weapons, collection: User.weapons, label: false %>
  <%= f.button :submit %>
  <% end %>

